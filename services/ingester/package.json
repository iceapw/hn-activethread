{
  "name": "@activethread/ingester",
  "version": "1.0.0",
  "private": true,
  "description": "Data ingester — polls HackerNews Firebase API and publishes to Kafka",
  "main": "src/index.js",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js"
  },
  "dependencies": {
    "kafkajs": "^2.2.4"
  }
}
